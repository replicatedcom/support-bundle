analyze:
  v1:
    - name: os.empty
      registerVariables:
        - name: os
          os: {}
      condition:
        empty: {}
        variableRef: os
      messages:
        conditionError:
          primary: OS empty is error
          detail: Detect OS
          severity: error
        conditionFalse:
          primary: OS empty is false
          detail: Detect OS
          severity: error
        conditionTrue:
          primary: OS empty is true
          detail: Detect OS
          severity: info
    - name: os.empty.eval
      registerVariables:
        - name: os
          os: {}
      condition:
        eval: '{{repl not .os }}'
        variableRef: os
      messages:
        conditionError:
          primary: OS empty is error
          detail: Detect OS
          severity: error
        conditionFalse:
          primary: OS empty is false
          detail: Detect OS
          severity: error
        conditionTrue:
          primary: OS empty is true
          detail: Detect OS
          severity: info
