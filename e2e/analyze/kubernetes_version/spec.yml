collect:
  v1:
    - kubernetes.version: {}
      output_dir: /kubernetes/version
      meta:
        labels:
          analyze: kubernetes-version

analyze:
  v1alpha1:
    - kubernetes.version:
        semver_minimum: 1.10.0
      collect_refs:
        - selector:
            analyze: kubernetes-version
