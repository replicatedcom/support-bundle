collect:
  v1:
    - docker.version: {}
      output_dir: /docker/version
      meta:
        labels:
          analyze: docker-version

analyze:
  v1:
    - docker.version:
        server_version_minimum: 17.06.2
      collect_refs:
        - selector:
            analyze: docker-version
